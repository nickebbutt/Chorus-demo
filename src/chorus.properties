# Properties set in the chorus.properties at the root of the classpath are available to all features
#
# This is a good place to set defaults for Chorus' handlers using the special 'default' properties groups
#
# e.g.
# processes.default sets defaults for all processes handler configurations
# remoting.default sets defaults for all remoting handler configurations

# default logging to true for the processes handler
processes.default.logging=true